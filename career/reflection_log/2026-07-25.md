# 2026-07-25 — Learning session (worksheet §1.3, interrupted)

Worked the §1.3 worksheet (`career/lesson_assignments/2026-07-20_zero-few-shot-role-prompting.py`) — 16/19 green. Exercises 1–4 all pass; Part 3 (triage call) has 3 fails left. Coach notes with the exact fixes are embedded in the file next to the `build_triage_call` TODO.

Debugging wins this session: reversed `.join()` receiver, missing `return`, plain-dict `KeyError` vs `defaultdict`, and collapsing a count-dict to `len(set(x)) > 1` (distinctness, not counts). Concept banked: stable vs volatile — stable content (role + examples) rides the cacheable system prefix, volatile input goes last.

**Stopped at:** Part 3 `build_triage_call`, 3 fails (schema fields-vs-values mixup, missing enum, `review_comment` unused). **Next step:** read the COACH NOTES block in the file, fix the three, run `python3` on the file — done = 19/19 green.
